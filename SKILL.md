---
name: video-subtitle
description: 视频字幕自动生成与优化。流程：视频 → faster-whisper 转录 → 智能断句分割 → 领域术语纠错 → 输出 SRT。
---

# Video Subtitle Generator

从视频或音频文件自动生成高质量字幕，包含转录、智能断句、术语纠错全流程。

## 适用场景

- "给我这个视频做字幕"
- "这个字幕断句太长，帮我切短一点"
- "批量处理这些视频的字幕"
- "字幕里有术语错误，帮我修正"

---

## 工作流

```
Phase 1   确认输入
   1.1  确认视频/音频文件路径
   1.2  确认 faster-whisper 项目路径（含本地模型）
   1.3  确认领域类型（默认：德州扑克，可选：通用/自定义）

Phase 2   转录（可选）
   2.1  如果已有 SRT → 跳到 Phase 3
   2.2  用 faster-whisper large-v3 转录音频

Phase 3   智能断句
   3.1  按标点分割（。！？；，）
   3.2  按虚词分割（的了吗呢吧啊呀嘛着了过）
   3.3  按关联词分割（但是/所以/如果/因为/虽然/而且）
   3.4  **英文单词保护**：分割前检查是否切断英文单词，避免 deny→de/ny
   3.5  控制每行 ≤16 字符
   3.6  时间戳按比例分配
   3.7  合并过短碎句

Phase 4   术语纠错
   4.1  根据领域应用纠错字典

Phase 5   质量审查与修复
   5.1  检测问题行：单字行、纯标点行、极短行（≤2字）
   5.2  检测行首标点（，。！？;等）
   5.3  检测截断英文（如 on, er, se, ker 等）
   5.4  检测逗号结尾的短行（≤5字且无完整主谓结构）
   5.5  **检测被断开的英文单词**（行尾半词+下行首半词=完整词，如 de+ny=deny）
   5.6  行首标点：强制与前一行合并并重分
   5.7  合并到前/后一行（如合并后≤16字）
   5.8  三行合并重分：前后超长时合并前+当前+后，按标点重分
   5.9  多轮迭代直到无问题行

Phase 6   时间戳修复
   6.1  检测时间戳重叠（当前行结束 > 下一行开始）
   6.2  将当前行结束时间设为下一行开始时间
   6.3  确保每行至少显示 300ms

Phase 6   时间戳修复
   6.1  检测时间戳重叠（当前行结束 > 下一行开始）
   6.2  将当前行结束时间设为下一行开始时间
   6.3  确保每行至少显示 300ms

Phase 7   英文单词修复
   7.1  检测被断开的英文单词（行尾半词+下行首半词=完整词）
   7.2  合并断裂单词到同一行
   7.3  合并后超长时重新分割

Phase 8   **人工审核（必须，不可跳过）**
   8.1  **亲自逐文件读取字幕内容**，不要依赖脚本自动检查
   8.2  **结合上下文理解语义**，识别语音识别错误
   8.3  常见错误类型：
        - 同音词错误（抵持→底池、nas→nuts、Flob→flop）
        - 英文术语识别错误（Tekris→check raise、Sight→sit、Full→fold）
        - 专有名词错误（stab 是刺击，不是 3-bet；Sauer 是 solver 变体）
        - 字母数字混淆（Race→raise、Ray→raise、Cby→c-bet）
   8.4  **脚本只能检查预定义规则**，无法覆盖所有情况
   8.5  **必须理解上下文**才能判断：
        - 哪个词在上下文中不合理
        - 结合领域知识推测正确词汇
        - 验证修正后是否符合语境
   8.6  修改后再次检查，确保无新问题引入

Phase 9   输出
   9.1  SRT 文件输出到 final_output/
   9.2  报告处理统计
```

---

## 使用方式

```bash
# 转录视频并生成字幕
python process.py --input video.mp4 --fw-dir /path/to/faster-whisper

# 处理已有 SRT 文件
python process.py --input /path/to/srt_folder

# 自定义每行字符数
python process.py --input video.mp4 --fw-dir /path/to/faster-whisper --max-chars 20

# 使用通用纠错（不应用特定领域规则）
python process.py --input video.mp4 --fw-dir /path/to/faster-whisper --domain generic
```

### 质量审查（修复问题行）

```bash
python review_and_fix.py --input ./corrected_subtitles
```

### 修复时间戳重叠

```bash
python fix_timestamps.py --input ./final_subtitles
```

### 修复英文单词断裂

```bash
python fix_broken_words.py --input ./final_subtitles
```

---

## 文件结构

```
video-subtitle/
├── SKILL.md              # 本文件
├── process.py            # 主处理脚本（转录+断句+纠错）
├── review_and_fix.py     # 质量审查与修复工具
├── fix_timestamps.py     # 时间戳重叠修复工具
├── requirements.txt      # 依赖
└── README.md             # 项目说明
```

## 质量审查规则

### 问题行定义
1. **极短行**：≤2 字符
2. **纯标点行**：仅包含标点符号
3. **行首标点**：以 ，。！？; 等开头
4. **截断英文**：1-4 字母（如 on, er, se, ker）
5. **短逗号行**：≤5 字且以逗号结尾，且无完整主谓结构

### 修复策略（按优先级）
1. **行首标点**：强制与前一行合并，三行重分
2. **合并到前一行**：合并后 ≤16 字则合并
3. **合并到后一行**：合并后 ≤16 字则合并
4. **三行合并重分**：合并前+当前+后，按标点分割成 ≤16 字
5. **多轮迭代**：最多 50 轮，直到无问题行

---

## ⚠️ 人工审核原则（核心规则）

### 为什么必须人工审核
1. **脚本只能检查预定义的规则**，无法覆盖所有语音识别错误
2. **语音识别错误千奇百怪**，同音词、近音词、专业术语变形无法穷举
3. **同一个词在不同上下文中含义不同**（如 stab 是刺击，不是 3-bet）
4. **自动检查会误判**（如把正确的 stab 误改成 3-bet）

### 人工审核方法
1. **逐文件读取**：不要只看脚本输出的问题列表
2. **理解上下文**：把字幕当作完整文本来读，不要逐行孤立判断
3. **识别不合理之处**：
   - 读起来不顺畅的词
   - 不符合领域术语的表达
   - 语法不通的句子
4. **结合领域知识修正**：
   - 德州扑克术语：底池、筹码、check、raise、call、fold、bluff、equity、nuts 等
   - 英文术语保持原样：solver、c-bet、3-bet、flop、turn、river 等
   - 专有名词不要改：stab（刺击）、blocker（阻挡牌）、SPR 等
5. **修正后验证**：
   - 修正后的词在上下文中是否合理
   - 是否引入了新的错误
   - 时间戳是否仍然同步

### 历史错误教训
| 错误修改 | 正确做法 | 教训 |
|---------|---------|------|
| stab → 3-bet | stab 是刺击（偷池） | 不要改不懂的术语 |
| NAS → nuts（部分正确） | 检查上下文确认 | 同音词要结合语境 |
| Race → raise（正确） | 但 Ray → raise 也正确 | 需要看上下文 |

### 审核流程
```
1. 读取整个 SRT 文件内容
2. 通读一遍，标记不合理之处
3. 结合上下文分析每个标记
4. 应用修正
5. 再读一遍确认无新问题
```
